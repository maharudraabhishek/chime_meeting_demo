package com.example.chimemeeting.chime_meeting_demo

/**
 * Retained as a non-executable migration marker because repository cleanup is
 * non-destructive. The manifest launches `com.example.chimemeeting.MainActivity`;
 * no Android component is declared in this legacy package.
 */
